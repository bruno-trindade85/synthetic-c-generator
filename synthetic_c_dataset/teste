#include <stdlib.h>
#include <string.h>

void leaky() {
    char *p = (char*) malloc(10);
    strcpy(p, "123456789"); // ok, mas sem free → alerta de leak
}

int main() {
    leaky();
    return 0;
}

